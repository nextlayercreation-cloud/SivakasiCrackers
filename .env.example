# Copy this file to .env if you need to point the React app at a
# different backend URL (e.g. when deploying frontend and backend
# separately). Not required for local development — the app
# defaults to http://localhost:5000/api.

REACT_APP_API_URL=http://localhost:5000/api
